<h1>New Episode</h1>

<%= form_for ([@podcast, @episode]) do |f| %>

	<div class="form-group">
		<%= f.text_field :title, placeholder: "Title", class: "form-control" %>
	</div>

	<div class="form-group">
		<%= f.text_area :description, placeholder: "Tell us about this episode", class: "form-control" %>
	</div>

	<div class="actions">
    <%= f.submit "Create Episode", class: "btn btn-primary" %>
  </div>

<% end %>